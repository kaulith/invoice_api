meta {
  name: Get a single item.
  type: http
  seq: 1
}

get {
  url: {{base_url}}/items/{{item_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  item_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
