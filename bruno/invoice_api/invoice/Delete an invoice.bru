meta {
  name: Delete an invoice.
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/invoices/api/{{invoice_id}}
  body: none
  auth: inherit
}

vars:pre-request {
  invoice_id: 1
}

settings {
  encodeUrl: true
  timeout: 0
}
